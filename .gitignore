# データベースファイル（PostgreSQLへ移行済みのため不要）
/SQL/opendata.sqlite
/SQL/backup.sql
/SQL/log_archives/
/SQL/

bfg-1.15.0.jar

# 環境変数（認証情報が含まれるため絶対に公開しない）
.env
init_sqlite.php

# PHP依存パッケージ（サーバー側で composer install するため不要）
/vendor/

/storage/cache

# OSやエディタのキャッシュファイル
.DS_Store
/node_modules/
.vscode/