# 開発環境設定 (local / production)
APP_ENV=local

# データベースファイルのパス
# Docker内での絶対パスを指定するのが確実です。
# デフォルトの構成なら /var/www/html がルートなので、そこからのパスになります。
DB_PATH=/var/www/html/SQL/opendata.sqlite

# デバッグモード (true/false)
APP_DEBUG=true